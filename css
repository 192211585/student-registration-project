body {
    background-color: #f2f2f2;
    text-align: center;
    font-family: Arial;
}

input, select, textarea {
    padding: 8px;
    width: 250px;
}

button {
    padding: 10px 20px;
    background-color: green;
    color: white;
    border: none;
    cursor: pointer;
}

table {
    margin: auto;
    margin-top: 20px;
}
